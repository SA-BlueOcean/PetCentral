export default function EditPets({ profileId }: { profileId: string }) {
  return (
    <dialog id="my_modal_7" className="modal">
      <div className="modal-box">
        <form method="dialog">
          <button className="btn btn-sm absolute right-5 top-5">&times;</button>
        </form>
      </div>
    </dialog>
  );
}
